

// TODO: make a proper alias for char, even though we dont want care about it being strongly type checked
func to_lower(char: u8) u8
{
    if(char >= 'A' && char <= 'Z')
    {
        char -= 0x20;
    }

    return char;
}

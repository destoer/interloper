import "system"

// TODO: we need to define this for each arch
func start()
{
    init_global();

    rc : s32 = main();
    exit(rc);
}

// TODO: should this just be kept internally?
func init_global()
{

}
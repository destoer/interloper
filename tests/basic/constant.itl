/*
struct Point
{
    x : u32;
    y : u32;
};

constant TABLE : u32[?] = {1,2,3,4,5};

constant point : Point = {3,5};

func sum_arr() s32
{
    ans : s32 = 0;

    for(i : u32 = 0; i < TABLE.len; i++)
    {
        ans += TABLE[i];
    }

    return ans;
}
*/

// expected 32
func main() s32
{
/*
    sum := sum_arr(TABLE);

    product := point.x * point.y;
*/
    return u32.size * BITS_IN_BYTE;
}

// expected 1190 (0)

func main() s32
{
    x : s32 = (2 + 2 / 2 * 5) - 1;

    y : s32;
    
    y = x;

    z : s32 = x + y + 2;

    // Do some useless store / load
    y = z;
    x = y;

    y = z * 5;
    x = y;
    a : s32 = x;

    v : s32 = (((a * x) / (y * 5)) * ((x + z) - 5));

    ans := z + x + v;

    return ans - 1190;
}
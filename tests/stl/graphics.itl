
import <graphics>

func main() s32
{
    win := open_window("interloper",640,480);

    quit : bool = false;

    while(!quit)
    {
        proccess_events(&win);

        sleep_ms(16);
    }

    close_window(&win);

    return 0;
}


func main() s32
{
    [buf, err] := read_bin("tests/stl/lines.txt");

    if(err)
    {
        return -1;
    }

    write_str_raw(cast(c8@,&buf[0]),buf.len);
    write_string("\n");

    // free buffer
    free_heap(&global_heap,&buf.data);

    return cast(s32,buf.len >= 57);
}
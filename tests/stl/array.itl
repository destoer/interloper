

func main() s32
{
    arr : Array;

    
    for(i in 1 <= 4)
    {
        push_array<u32>(&arr,i);
    }

    linefeed();

    for(i in 0 < 4)
    {
        if(read_array<u32>(&arr,i) != i + 1)
        {
            destroy_array(&arr);
            return 0;
        }
    }

    destroy_array(&arr);

    return 1;
}
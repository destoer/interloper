

func main() s32
{
    arr : Array;

    
    for(i in 1 <= 4)
    {
        push_arr<u32>(&arr,i);
    }

    linefeed();

    for(i in 0 < 4)
    {
        if(read_arr<u32>(&arr,i) != i + 1)
        {
            print("{}\n",read_arr<u32>(&arr,i));
            destroy_arr(&arr);
            return 0;
        }
    }

    destroy_arr(&arr);

    return 1;
}
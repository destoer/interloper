struct Point
{
    s32 x = 2;
    s32 y = 5;
};

// make sure returns are working
func Point make_point(s32 x, s32 y)
{
    const Point p = {x,y};
    return p;
}

// pass one in as a pointer just to check deref works
func s32 calc_gradient(Point v1, Point@ v2)
{
    return (v2.y - v1.y) / (v2.x - v1.x);
}

func s32 main()
{
    Point v1; // test default values
    Point v2 = make_point(3,7);

    return calc_gradient(v1,&v2);  
}


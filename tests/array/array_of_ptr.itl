

// expected 6

func main() s32
{
    x : u32 = 0;
    y : u32 = 0;
    z : u32 = 0;

    buf : u32[3]@ = {&x,&y,&z};

    for(i : u32 = 0; i < buf.len; i += 1)
    {
        @buf[i] = i + i;
    }

    return x + y + z;
}
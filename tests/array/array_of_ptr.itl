

// expected 6

func main() s32
{
    u32 x = 0;
    u32 y = 0;
    u32 z = 0;

    u32@[3] buf = {&x,&y,&z};

    for(u32 i = 0; i < buf.len; i += 1)
    {
        @buf[i] = i + i;
    }

    return x + y + z;
}
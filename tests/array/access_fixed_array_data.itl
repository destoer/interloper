
func main() s32
{
    arr: s32[?] = {1,2,3,4};
    data := arr.data;

    sum: s32 = 0;
    for(i in 0 < arr.len)
    {
        sum += data[i];
    }

    ans := sum == 10;

    return cast(s32,ans);
}
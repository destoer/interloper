

// expected 1061

func main() s32
{
    x: bool[100];

    // TODO: replace with initializer
    for(i : u32 = 0; i < x.len; i += 1)
    {
        x[i] = true;
    }

  
    for(i : u32 = 2; i < x.len; i += 1)
    {
        if(x[i])
        {
            for(j : u32 = i * 2; j < x.len; j += i)
            {
                x[j] = false;      
            }
        }
    }

    ans : u32 = 0;
    for(i : u32 = 0; i < x.len; i += 1)
    {
        if(x[i])
        {
            ans += i;
        }
    }

    return ans;
}



#attr(flag)
enum defer_flag : u32
{
    none
}

func defer_test(flags: u32, buf: s32[])
{
    x = 1;
    index: u32 = 0;

    defer buf[index] = x 
}

func main() s32
{
    buf: s32[32];

    defer_test(defer_flag::none);

    res1 := buf[0] == 1;
    ans := res1;

    return cast(s32,ans);
}
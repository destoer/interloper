

func check_val(v : u64, sign : bool, len : s64, value : Value, len_out : s64) bool
{
    return value.v == v && sign == value.sign && len == len_out;
}

func main() s32
{
    [value, len] := parse_value("55");

    res1 := check_val(55,false,2,value,len);

    return cast(s32,res1);
}
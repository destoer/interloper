
// expected 7
import "basic"

func s32 main()
{
    u8[?] str = "Hello there";

    u32 set = 0;

    for(u32 i = 0; i < str.len; i += 1)
    {
        str[i] = to_lower(str[i]);

        u8 x = str[i] - 'a';

        set = set_bit(set,x);
    }

    return hamming_weight(set);
}

// expected 7

func main() s32
{
    str : u8[?] = "Hello there";

    set : u32 = 0;

    for(i : u32 = 0; i < str.len; i += 1)
    {
        str[i] = to_lower(str[i]);

        x : u8 = str[i] - 'a';

        set = set_bit(set,x);
    }

    return hamming_weight(set);
}